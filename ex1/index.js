const funcs = require('./Functions');

const main = () => {
    //funcs.create(2, "hello    22222");
    //funcs.read(2);
    //console.log(funcs.getRandNumber());

    funcs.create(1, "abcdefg hijk");
    funcs.concatFiles();
};


main();